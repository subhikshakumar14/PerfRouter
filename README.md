# PerfRouter
This site is a graph-based pathfinding system designed to automate the layout of jumper wires on a perf board to eliminate the "messy" appearance of manual prototyping. By treating the board as a grid, the system calculates the most efficient, non-overlapping routes for connections based on user-provided pin configurations.
